<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List Cars</title>
    <link rel="stylesheet" href="/stylesheets/list.css">
</head>
<body>
    {{> navbar}}
    <div class="container-list">
        {{#each coches}}
            <div class="card">
                <h1>{{name}}</h1>
                <h1>{{model}}</h1>
                <img src="{{image}}">
                <div class="buttons">
                    <button class="editar"><a href="/update-car/{{_id}}">Editar</a></button>
                    <form action="/delete-car/{{_id}}?_method=DELETE" method="POST">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="eliminar">Eliminar</a></button>
                    </form>
                </div>
            </div>
        {{/each}}
    </div>
</body>
</html>